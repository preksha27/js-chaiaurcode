 console.log(null >0);
 console.log(null==0);
 console.log(null >=0);

 console.log(undefined ==0);
 // when used comparitor operator with null, it automatically gets converted to 0 i,e null = 0
 //so null(0)>0 is false and null(0)>=0 is true
 //thats why === is used at it is strict and it will not convert it to anything and also checks datatype

